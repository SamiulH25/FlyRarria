# `main` (scaffold)

By Samiul. Three commits underpinning everything after:

- `d377928` — Scaffold FlyRarria: tModLoader pet + LIF brain core +
  circuit extractor.
- `c35eb59` — Complete code to run-line: CircuitLoader, extractor queries,
  bench protocol, feeding/bond/perch, HUD, runbook.
- `cd86afb` — Drop pycache, ignore it.

Merged PR #1 on top as `3d04223` (whole-CNS brain, see `fly-brain.md`).
