# Samiul (SamiulH25 · `bob2142` · BOB2142)

## Owns

- Repo scaffold, tModLoader build/run pipeline, local setup (ModSources,
  headless `-build`, .NET 8 SDK).
- Merges and release flow.

## Branches worked

- `main` — full scaffold, commits `d377928`, `c35eb59`, `cd86afb`
  (see `../branches/main.md`).
- Merged PR #1 (`fly-brain` → `main`) as `3d04223`.
- `dev` — current branch (see `../branches/dev.md`).
