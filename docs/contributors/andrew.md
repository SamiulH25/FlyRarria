# Andrew Priestley (Voicedrew11)

## Owns

- Brain engine (`Brain/LifNetwork.cs`: event-driven LIF, sharding,
  short-term depression, WholeCns params).
- Connectome pipeline (`tools/extract_connectome.py`,
  `Brain/ConnectomeFile.cs`, KC-KC inversion, transmitter signs).
- Pet behaviour (`Content/Pets/MoteProjectile.cs` steering/senses,
  `Brain/MotorDecoder.cs` hysteresis, hunger/sugar gain).
- Debug surface (`Content/Debug/Neuroscope.cs`, `MoteHud.cs`,
  `MoteCommands.cs`) and validation (`tools/bench.py` protocol).

## Branches worked

- `fly-brain` — all 8 commits, PR #1 (see `../branches/fly-brain.md`).
